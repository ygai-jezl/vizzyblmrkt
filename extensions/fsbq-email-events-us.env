# firebase/firestore-bigquery-export@0.3.2 — email_events in us (DB (default) @ nam5) -> waitlist_us.email_events_raw_*
# Trigger region derives from DATABASE_REGION (0.3.x: triggerRegion=${DATABASE_REGION}).
COLLECTION_PATH=email_events
DATABASE=(default)
DATABASE_REGION=nam5
DATASET_ID=waitlist_us
DATASET_LOCATION=us
TABLE_ID=email_events
WILDCARD_IDS=false
BIGQUERY_PROJECT_ID=vizzybl-marketing-prod
