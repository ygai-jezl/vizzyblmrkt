import { StubPage } from "@/components/admin/StubPage";

export const dynamic = "force-dynamic";

export default function CrmPage() {
  return (
    <StubPage
      title="Unified CRM"
      description="A single view of every contact across all launches — auto-enriched profiles, waitlist position, and journey history in one place."
    />
  );
}
