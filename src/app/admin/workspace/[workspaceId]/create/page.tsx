import { StubPage } from "@/components/admin/StubPage";

export const dynamic = "force-dynamic";

export default function CreatePage() {
  return (
    <StubPage
      title="Create"
      description="The Hub Composer (long-form editor with a grounding-context sidebar that pulls from your curated knowledge base) and the Downstream Deconstructor that splits a finished Hub into platform-native spokes, with dynamic CTA tokens."
    />
  );
}
